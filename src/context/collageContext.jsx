import { createContext } from "react";

const collageContext = createContext("CodeGirls");
export const collageFee = createContext(1000);
export const authContext = createContext(false);

export default collageContext;